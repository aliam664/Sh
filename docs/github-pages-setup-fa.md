# 🌐 راهنمای سایت‌کردن بازی با GitHub Pages (یک‌بار، ۱ دقیقه)

بازی «دوئل رمز» از مسیر `docs/guess-duel/` ریپو سرو می‌شود و GitHub Pages می‌تواند آن را به یک **سایت زنده** تبدیل کند. همه‌چیز آماده است؛ فقط فعال‌سازی Pages نیاز به یک کلیک شما دارد (دسترسی توکن ربات به تنظیمات Pages محدود است).

---

## ✅ فعال‌سازی (فقط یک‌بار)

**با گوشی** یا کامپیوتر:

1. این لینک را باز کنید: <https://github.com/aliam664/Sh/settings/pages>
   - روی گوشی: در Chrome از منوی ⋮ گزینهٔ **Desktop site** را روشن کنید.
2. بخش **Build and deployment** → زیر **Branch**:
   - برنچ: **`arena/019fb378-sh`** را انتخاب کنید
   - پوشه: **`/docs`** را انتخاب کنید
3. **Save** را بزنید و ~۱ دقیقه صبر کنید. ✅

> بعداً هر وقت این برنچ را روی `main` مرج کردید، می‌توانید در همین صفحه برنچ را به `main` تغییر دهید.

## 🔗 آدرس‌های دائمی سایت (بعد از فعال‌سازی)

| صفحه | آدرس |
|---|---|
| ⚔️ خودِ بازی | **https://aliam664.github.io/Sh/guess-duel/** |
| 🏠 صفحهٔ اصلی سایت | https://aliam664.github.io/Sh/ |

## ⚡ پیش‌نمای فوری (بدون هیچ فعال‌سازی، همین الآن کار می‌کند)

چون ریپو عمومی است، این سرویس‌ها فایل‌های خام را زنده سرو می‌کنند:

- **پیشنهادی (بدون صفحهٔ میانی):** https://htmlpreview.github.io/?https://github.com/aliam664/Sh/blob/arena/019fb378-sh/docs/guess-duel/index.html
- جایگزین (اولین بار یک صفحهٔ هشدار با دکمهٔ Open the page نشان می‌دهد): https://raw.githack.com/aliam664/Sh/arena/019fb378-sh/docs/guess-duel/index.html

## 🔄 چطور به‌روزرسانی‌ها زنده می‌شوند؟

- Pages روی حالت **Deploy from a branch** تنظیم می‌شود؛ یعنی **هر پوش جدید** به این برنچ، ظرف ~۱ دقیقه به‌صورت خودکار روی سایت زنده می‌شود.
- پس اگر باگی گزارش کنید و فیکسش کنیم، کافی‌ست پوش انجام شود → لینک همان است، محتوا تازه می‌شود. هیچ کار اضافه‌ای لازم نیست.

## 🛠 جزئیات فنی

- سورس بازی: `docs/guess-duel/` (HTML/CSS/JS خالص، بدون هیچ وابستگی)
- صفحهٔ لندینگ: `docs/index.html`
- فایل `docs/.nojekyll` پردازش Jekyll را خاموش می‌کند تا فایل‌ها دقیقاً همین‌طور که هستند سرو شوند.
