نحوه اتصال و بازی آنلاین:
۱. Firebase Config را در js/firebase-config.js وارد کنید.
۲. Firebase Auth Anonymous و Firestore فعال باشند.
۳. GitHub Pages فعال شود.
۴. سایت بارگذاری شود و کاربر Anonymous Login کند.
۵. Lobby بسازد یا وارد Room با کد ۶ کاراکتری شود.
۶. بازی با onSnapshot لحظه‌ای همگام‌سازی می‌شود.

وضعیت فعلی پروژه: فایل‌های اولیه (index.html, firebase-config.js) ساخته شده‌اند اما هنوز database.js، room.js، game-engine.js، questions.js و Firebase Rules کامل نیستند. تا تکمیل این‌ها بازی آنلاین قابل اجرا نیست. پس تکمیل پروژه = قابل بازی شدن.
